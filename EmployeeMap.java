import java.util.Map;
import java.util.HashMap;

/**
   Chapter 18, Programming Challenge 3
   EmployeeMap Class
*/

public class EmployeeMap
{
   //Fields
   
   /**
      Constructor
   */
   
   public EmployeeMap()
   {
      // Instantiate the map.

   }
   
   /**
      The add method creates a mapping in the map.
      An employee ID number is mapped to an Employee
      object.
      @param e The Employee object to create the
               mapping with.
   */
   
   public void add(Employee e)
   {

   }
   
   /**
      The get method retrieves the Employee object
      that is associated with a specific ID number.
      @param idNumber An employee ID number.
      @return The Employee object associated with the
              specified ID number. Returns null if no
              mapping exists for the ID number.
   */
   
   public Employee get(String idNumber)
   {

   }
}